############
# Template for .env
# Copy to .env and fill in your values
# This file mirrors GitHub Secrets + Variables
# Shared config lives in .env.config (committed)
############


############
# Environment-specific (differ between local and prod)
# On prod: set as GitHub Variables with SB_ prefix
############

API_DOMAIN=localhost
STUDIO_DOMAIN=localhost
SITE_URL=http://localhost:3000
ADDITIONAL_REDIRECT_URLS=http://localhost:3000/**
API_EXTERNAL_URL=http://localhost:8000
SUPABASE_PUBLIC_URL=http://localhost:8000


############
# Secrets
# On prod: set as GitHub Secrets with SB_ prefix
# Generate before first use -- see docs/GITHUB-SETUP.md
############

POSTGRES_PASSWORD=your-super-secret-postgres-password
JWT_SECRET=your-super-secret-jwt-token-min-32-chars
ANON_KEY=your-generated-anon-jwt-key
SERVICE_ROLE_KEY=your-generated-service-role-jwt-key
DASHBOARD_PASSWORD=your-dashboard-password
# Traefik basic auth for Studio (htpasswd format, generate with: echo "$(htpasswd -nb supabase YOUR_PASSWORD)")
# IMPORTANT: Dollar signs must be doubled ($ -> $$) for Docker Compose
STUDIO_BASIC_AUTH=supabase:$$apr1$$placeholder$$placeholder
SECRET_KEY_BASE=your-secret-key-base-64-chars
VAULT_ENC_KEY=your-32-hex-char-encryption-key
PG_META_CRYPTO_KEY=your-32-hex-char-encryption-key
LOGFLARE_PUBLIC_ACCESS_TOKEN=your-logflare-public-token
LOGFLARE_PRIVATE_ACCESS_TOKEN=your-logflare-private-token
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
OPENAI_API_KEY=
